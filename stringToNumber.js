const stringToNumber = function(str){
    // Using parseInt to convert string to number
    return parseInt(str);
  }
  
  // Test the function
  console.log(stringToNumber("42")); // Output: 42
  console.log(stringToNumber("3.14")); // Output: 3

  //tasked with converting string to number so by using the parseInt method I could convert it over.